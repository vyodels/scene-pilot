cache=.npm-cache
legacy-peer-deps=true
# Keep install stable in this environment; enable scripts explicitly for
# release machines that need Electron binary downloads during packaging.
ignore-scripts=true
fetch-retries=5
fetch-retry-factor=2
fetch-retry-mintimeout=2000
fetch-retry-maxtimeout=20000
