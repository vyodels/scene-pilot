import defaultStateMachineJson from "./defaultStateMachine.json";
import type { RecruitmentStateMachine } from "../types/stateMachine";

export const defaultRecruitmentStateMachine = defaultStateMachineJson as RecruitmentStateMachine;

export default defaultRecruitmentStateMachine;
